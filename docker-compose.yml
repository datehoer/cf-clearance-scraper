services:
  cf-clearance-scraper:
    build: .
    # image: zfcsoftware/cf-clearance-scraper:latest
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      browserLimit: 20
      timeOut: 60000
      # Optional auth:
      # authToken: "your_auth_token"
      # Optional client key validation:
      # CLIENT_KEY: "your_client_key"
